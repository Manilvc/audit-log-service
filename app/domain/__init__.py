"""Domain model: taxonomy and the canonical audit event.

``enums``
    ECS-style actions, categories, outcomes, scopes — stable forever once written.
``events``
    Strict internal event shape and the PII field registry.
``legacy``
    Maps main-backend display strings ("Issue Credential (Bulk)") ↔ ECS actions.
"""
